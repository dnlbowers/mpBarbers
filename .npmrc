# Enforce package manager consistency
package-manager-strict=true

# Prevent npm from being used when yarn.lock exists
engine-strict=true

# Save exact versions (optional but recommended)
save-exact=true